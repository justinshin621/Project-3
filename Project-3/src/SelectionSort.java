/**
 * This class will sort the heap of records by selection.
 * 
 * @author justin, Andy
 *
 */
public class SelectionSort {
	private Heap minHeap;
	private byte[] inputBuffer;
	private byte[] outputBuffer;
	
	public SelectionSort(Heap heap) {
		minHeap = heap;
	}
	
	
}
